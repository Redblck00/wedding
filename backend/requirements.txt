fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
alembic==1.19.1

# psycopg 3, not psycopg2 -- DATABASE_URL must therefore start with
# postgresql+psycopg://
psycopg[binary]==3.3.4

pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0

# Auth: PyJWT + bcrypt directly. python-jose is unmaintained, and
# passlib[bcrypt] 1.7.4 crashes against bcrypt >= 4.1.
PyJWT==2.13.0
bcrypt==5.0.0

python-multipart==0.0.32
cloudinary==1.45.0
Pillow==12.3.0
qrcode[pil]==8.2
